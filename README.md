Installation
------------

`npm install`

Running
-------

`node index.js`

Implementation
--------------

Uses the `phantom.js` headless browser to monitor changes to the DOM, then outputs as necessary.

